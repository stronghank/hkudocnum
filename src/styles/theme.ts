'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#006699', // Adjust this color to match your school's theme
    },
    secondary: {
      main: '#19857b',
    },
  },
});

export default theme;